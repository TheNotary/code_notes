<?php
  // here's a comment
  // btw, put this hellow right on a server that has php installed and navigate to the page!
  // you will see all the text and values that follow the command "echo" and none of the other things
  
  echo "Hello world";
  
  $h = " HELLO ";      // variables
  $w = "WORLD";
  echo $h . $w . "!!!!";    // concatinating strings
  
  
  echo "<br>";
  echo 'you make me so "haPHP"';      // use single quote instead to contain doubles
  echo "<br>
    get it?
    <br>
    instead of happy, haPHP?
    <br>
    lol, I know, I'm clever<br>";  //  Loooong multi line echo
    
  echo "<br><br><br><br>************** LET's CRUNCH NUMBERS NOW! **************<BR>";
  
  $x = 21;     // more variables, these are integers
  $y = 2;
  $z = 43;
  
  echo "the first number is $x <br>";      // variable right in string
  
  $addition = $y + $z;  // crunch, crunch, crunch...
  
  echo "the second plus the third is $addition <br>";
  
  
  echo "<br><br><br><br>************** LET's SHOW ARRAY WORK! **************<BR>";
  
  
  
?>